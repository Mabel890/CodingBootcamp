let n = (Number(prompt('¿Cuántos números desea generar?: '))), arr = [], mayor = [];
    for (let i = 0; i < n; i++) {
        arr[i] = Math.floor
        (Math.random() * 51);

        arr.sort();
        mayor = arr[i];
    }
console.log(arr);
console.log(mayor);